package com.example.billyapp.core

import android.content.Context
import android.util.Log

fun simulateEncounter(context: Context) {
    Log.d("DebugUtils", "simulateEncounter() called (placeholder)")
}

fun testBatchSystem(context: Context) {
    Log.d("DebugUtils", "testBatchSystem() called (placeholder)")
}
