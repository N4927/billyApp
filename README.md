# 📚 BillyApp — Documentation

[![Docs](https://img.shields.io/badge/docs-Feature%20Architecture%20%7C%20Implementation-blue)](#)
[![Format](https://img.shields.io/badge/format-LaTeX%20%7C%20PDF-orange)](#)
[![CI](https://img.shields.io/badge/CI-README%20Sync%20%7C%20Validation-brightgreen)](#)
[![Owner](https://img.shields.io/badge/Org-BillyApp%20Corp-black)](#)

Central repository for **feature-level documentation** of the BillyApp ecosystem.  

Each **top-level folder** in this repo represents **one app feature** (e.g. `ble/`), and each feature folder contains all the **architecture** and **implementation** documents for that feature, versioned over time.

---

## 🧱 Concept & Conventions

### Feature folders

At the root of the repo, each directory is a **feature** of BillyApp:

```text
.
├── ble/              # "BLE feature" documentation (example)
├── <next-feature>/   # future feature folders
├── LICENSE.txt
└── README.md
````

**Rules**

* One folder per feature (e.g. `ble`, `chat`, `backend-api`).
* Folder name is **lowercase**, **kebab_case** or **snake_case**.
* Everything strictly related to that feature’s docs lives under its feature folder.

---

### Document areas inside a feature

Inside each feature folder, we group documents by **area** (architecture, implementation, etc.).

Typical **area examples**:

* `architecture_client_server` — high-level architecture, sequence diagrams, responsibilities.
* `implementation_client` — low-level client implementation details, class diagrams, flows.
* `implementation_server` — backend-side details (if applicable).
* `operations` — runbooks, SLOs, operational readiness, etc.

---

### Versioned document directories

Each **document area** contains one or more **version directories**, each named:

```text
v_<MAJOR>_<MINOR>_<PATCH>
```

Examples:

* `v_1_0_0`
* `v_1_1_0`
* `v_2_0_0`

Inside each version directory you’ll typically find:

* `.tex` source (single entry point per document)
* `.pdf` export (canonical rendered output for that version)

> ✅ **Always consider the highest semantic version as the “current” doc for that area.**
> Older versions remain for historical context and audits.

---

## 📂 Feature Index

This section is kept in sync automatically by CI to reflect the actual folder structure in the repo.

<!-- FEATURE_INDEX:START - Do not edit this section manually. It is auto-generated by .github/scripts/update_readme.py -->




| Feature | Area / Document | Latest Version | Path |
|--------|-----------------|----------------|------|
| `ble` | `architecture_client_server` | `v_1_1_1` | `ble/architecture_client_server/v_1_1_1` |
| `ble` | `implementation_client` | `v_1_0_0` | `ble/implementation_client/v_1_0_0` |




<!-- FEATURE_INDEX:END -->

As you add new features or new versions under existing features, this table will be updated by the GitHub Actions workflows described below.

---

## 📄 Working with LaTeX & PDFs

This repo is designed to be friendly for LaTeX-based workflows.

### LaTeX toolchain

You can use any standard LaTeX distribution:

* **TeX Live** (Linux/macOS)
* **MacTeX** (macOS)
* **MiKTeX** (Windows)
* Editors: VS Code + LaTeX Workshop, Texmaker, TeXstudio, etc.

From a given version folder, compile the main `.tex` file, for example:

```bash
cd ble/architecture_client_server/v_1_1_0

# using latexmk
latexmk -pdf architettura_ble_lato_client_e_server.tex

# or standard pdflatex
pdflatex architettura_ble_lato_client_e_server.tex
biber    architettura_ble_lato_client_e_server  # only if you use biblatex/biber
pdflatex architettura_ble_lato_client_e_server.tex
pdflatex architettura_ble_lato_client_e_server.tex
```

The resulting `*.pdf` stays committed alongside the source.

---

## 🤖 Automation & CI/CD

This repo includes **GitHub Actions** for a lightweight, corporate-style docs pipeline.

### What the CI does

1. **Keeps the feature index in this README in sync** with the actual folder structure:

   * The script `.github/scripts/update_readme.py` scans the repo for feature & version folders and regenerates the table under `📂 Feature Index`.
2. **Validates changes on pull requests**:

   * If you add/remove features or versions but don’t update README, the **PR check will fail**.
3. **Auto-syncs README on `main`**:

   * After merging to `main`, CI runs the script and **commits any required README changes** back to the branch.

> This avoids manual, error-prone documentation of which features and versions are present.

---

## 🔧 Local workflow (when adding docs)

1. **Create or update a feature folder**

```bash
mkdir -p new_feature/architecture_system/v_1_0_0
```

2. **Add your LaTeX source and build**

```bash
cp template.tex new_feature/architecture_system/v_1_0_0/architettura_new_feature.tex
cd new_feature/architecture_system/v_1_0_0
latexmk -pdf architettura_new_feature.tex
```

3. **Commit everything except LaTeX aux files**

```bash
git add new_feature
git commit -m "docs(new_feature): add architecture v_1_0_0"
```

---

## 📜 License

See `LICENSE.txt` for licensing details.